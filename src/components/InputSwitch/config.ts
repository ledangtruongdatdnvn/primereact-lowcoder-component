import { NameConfig } from 'lowcoder-sdk';
export const staticProps = { tooltip: 'Switch me!' };
export const defaultChecked = true;
export const exposingConfigs = [new NameConfig('staticProps', 'static props of inputswitch'), new NameConfig('checked', 'checked props of inputswitch')];
