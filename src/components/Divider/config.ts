import { NameConfig } from 'lowcoder-sdk';
export const staticProps = {
  layout: 'horizontal',
  type: 'dashed',
};

export const exposingConfigs = [new NameConfig('staticProps', 'static props of divider')];
