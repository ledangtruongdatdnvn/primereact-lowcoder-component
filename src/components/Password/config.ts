import { NameConfig } from 'lowcoder-sdk';
export const staticProps = { tooltip: 'Enter a password' };
export const defaultValue = '';
export const exposingConfigs = [new NameConfig('staticProps', 'static props of password'), new NameConfig('value', 'value props of password')];
