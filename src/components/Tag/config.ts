import { NameConfig } from 'lowcoder-sdk';
export const staticProps = { severity: 'success' };

export const defaultValue = 'Success';

export const exposingConfigs = [new NameConfig('staticProps', 'static props of timeline'), new NameConfig('value', 'value props of timeline')];
